# Finnhub (https://finnhub.io) — free tier API key. Used only on the server in /api/quotes.
# Copy to `.env.local` for local dev. On Vercel, add the same variable in Project Settings → Environment Variables.
FINNHUB_API_KEY=
