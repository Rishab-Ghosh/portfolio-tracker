# ─── Finnhub (server-only) ─────────────────────────────────────────────────
# Used by GET /api/portfolio for daily candles + latest quotes (SPY + book).
# Sign up: https://finnhub.io/register
#
# Local: copy this file to `.env.local` and paste your key.
# Vercel: Project → Settings → Environment Variables → add for Production + Preview.
#
# The key is read only on the server (`src/lib/finnhub.ts`, `src/app/api/portfolio/route.ts`).
# It is never sent to the browser.

FINNHUB_API_KEY=
